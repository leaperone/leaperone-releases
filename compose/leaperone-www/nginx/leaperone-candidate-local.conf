# LEAPERone staged candidate routing on the Germany origin.
# Root traffic remains on the legacy Web while the final Dashboard hostname
# and a temporary WWW hostname are exercised with real browser/OAuth traffic.

server {
    listen 443 ssl http2;
    server_name leaper.one www.leaper.one;

    ssl_certificate     /etc/nginx/ssl/leaper.one/fullchain.cer;
    ssl_certificate_key /etc/nginx/ssl/leaper.one/leaper.one.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    include /etc/nginx/snippets/cloudflare-real-ip.conf;
    proxy_buffering off;

    location / {
        proxy_pass http://127.0.0.1:9800;
        proxy_set_header Host $http_host;
        include /etc/nginx/snippets/leaperone-proxy-headers.conf;
        client_max_body_size 20m;
    }
}

server {
    listen 443 ssl http2;
    server_name next.leaper.one;

    ssl_certificate     /etc/nginx/ssl/leaper.one/fullchain.cer;
    ssl_certificate_key /etc/nginx/ssl/leaper.one/leaper.one.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    include /etc/nginx/snippets/cloudflare-real-ip.conf;
    proxy_buffering off;

    location / {
        proxy_pass http://127.0.0.1:9820;
        proxy_set_header Host $http_host;
        include /etc/nginx/snippets/leaperone-proxy-headers.conf;
        client_max_body_size 20m;
    }
}

server {
    listen 443 ssl http2;
    server_name dashboard.leaper.one;

    ssl_certificate     /etc/nginx/ssl/leaper.one/fullchain.cer;
    ssl_certificate_key /etc/nginx/ssl/leaper.one/leaper.one.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    include /etc/nginx/snippets/cloudflare-real-ip.conf;
    proxy_buffering off;

    location / {
        proxy_pass http://127.0.0.1:9821;
        proxy_set_header Host $http_host;
        include /etc/nginx/snippets/leaperone-proxy-headers.conf;
        client_max_body_size 20m;
    }
}

server {
    listen 443 ssl http2;
    server_name api.leaper.one;

    ssl_certificate     /etc/nginx/ssl/leaper.one/fullchain.cer;
    ssl_certificate_key /etc/nginx/ssl/leaper.one/leaper.one.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    include /etc/nginx/snippets/cloudflare-real-ip.conf;
    proxy_buffering off;

    location / {
        proxy_pass http://127.0.0.1:9801;
        proxy_set_header Host $http_host;
        include /etc/nginx/snippets/leaperone-proxy-headers.conf;
        client_max_body_size 50m;
    }
}
