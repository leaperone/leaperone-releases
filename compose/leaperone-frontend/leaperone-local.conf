# LEAPERone final split routing on the Germany origin.
#
# Public site: leaper.one / www.leaper.one -> WWW :9820
# Dashboard:   dashboard.leaper.one         -> Dashboard :9821
# API:         api.leaper.one               -> API :9801
#
# The only root-domain exceptions are exact legacy payment callbacks. They
# protect already-created orders and external Stripe configuration without
# retaining the legacy Web, CLI, EnvX, Auth, or candidate routing layers.

server {
    listen 443 ssl http2;
    server_name leaper.one www.leaper.one;

    ssl_certificate     /etc/nginx/ssl/leaper.one/fullchain.cer;
    ssl_certificate_key /etc/nginx/ssl/leaper.one/leaper.one.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    include /etc/nginx/snippets/cloudflare-real-ip.conf;
    proxy_buffering off;

    location = /api/recharge/notify/stripe {
        proxy_pass http://127.0.0.1:9801/webhooks/payments/stripe;
        proxy_set_header Host api.leaper.one;
        include /etc/nginx/snippets/leaperone-proxy-headers.conf;
    }

    location = /api/recharge/notify/alipay {
        proxy_pass http://127.0.0.1:9801/webhooks/payments/alipay;
        proxy_set_header Host api.leaper.one;
        include /etc/nginx/snippets/leaperone-proxy-headers.conf;
    }

    location / {
        proxy_pass http://127.0.0.1:9820;
        proxy_set_header Host $http_host;
        include /etc/nginx/snippets/leaperone-proxy-headers.conf;
        client_max_body_size 20m;
    }
}

server {
    listen 443 ssl http2;
    server_name dashboard.leaper.one;

    ssl_certificate     /etc/nginx/ssl/leaper.one/fullchain.cer;
    ssl_certificate_key /etc/nginx/ssl/leaper.one/leaper.one.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    include /etc/nginx/snippets/cloudflare-real-ip.conf;
    proxy_buffering off;

    location / {
        proxy_pass http://127.0.0.1:9821;
        proxy_set_header Host $http_host;
        include /etc/nginx/snippets/leaperone-proxy-headers.conf;
        client_max_body_size 20m;
    }
}

server {
    listen 443 ssl http2;
    server_name api.leaper.one;

    ssl_certificate     /etc/nginx/ssl/leaper.one/fullchain.cer;
    ssl_certificate_key /etc/nginx/ssl/leaper.one/leaper.one.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    include /etc/nginx/snippets/cloudflare-real-ip.conf;
    proxy_buffering off;

    location / {
        proxy_pass http://127.0.0.1:9801;
        proxy_set_header Host $http_host;
        include /etc/nginx/snippets/leaperone-proxy-headers.conf;
        client_max_body_size 50m;
    }
}
