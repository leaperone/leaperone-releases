name: Deploy 2SOMEone

on:
  repository_dispatch:
    types: [deploy-twosomeone]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    outputs:
      pr_number: ${{ steps.set-ref.outputs.pr_number }}
      is_deployable: ${{ steps.set-ref.outputs.is_deployable }}
      deploy_env: ${{ steps.set-ref.outputs.deploy_env }}

    steps:
      - name: Validate and set ref
        id: set-ref
        run: |
          REF="${{ github.event.client_payload.ref }}"
          ENV="${{ github.event.client_payload.env }}"

          # Default deploy environment
          if [ -n "$ENV" ]; then
            DEPLOY_ENV="$ENV"
          else
            DEPLOY_ENV="production"
          fi

          if [ -z "$REF" ]; then
            echo "ref=main" >> $GITHUB_OUTPUT
            echo "pr_number=" >> $GITHUB_OUTPUT
            echo "is_deployable=true" >> $GITHUB_OUTPUT
            echo "deploy_env=$DEPLOY_ENV" >> $GITHUB_OUTPUT
            echo "使用默认分支: main (ref 为空)"
          elif [[ "$REF" == "main" ]] || [[ "$REF" == "refs/heads/main" ]]; then
            echo "ref=$REF" >> $GITHUB_OUTPUT
            echo "pr_number=" >> $GITHUB_OUTPUT
            echo "is_deployable=true" >> $GITHUB_OUTPUT
            echo "deploy_env=$DEPLOY_ENV" >> $GITHUB_OUTPUT
            echo "使用 main 分支: $REF"
          elif [[ "$REF" =~ ^([0-9]+)/merge ]]; then
            PR_NUMBER="${BASH_REMATCH[1]}"
            echo "ref=refs/pull/${PR_NUMBER}/merge" >> $GITHUB_OUTPUT
            echo "pr_number=${PR_NUMBER}" >> $GITHUB_OUTPUT
            echo "is_deployable=false" >> $GITHUB_OUTPUT
            echo "deploy_env=beta" >> $GITHUB_OUTPUT
            echo "检测到 PR merge ref，使用 PR #${PR_NUMBER} 的 merge ref"
          elif [[ "$REF" == *"/merge"* ]]; then
            PR_NUMBER=$(echo "$REF" | grep -oE '[0-9]+' | head -1)
            if [ -n "$PR_NUMBER" ]; then
              echo "ref=refs/pull/${PR_NUMBER}/merge" >> $GITHUB_OUTPUT
              echo "pr_number=${PR_NUMBER}" >> $GITHUB_OUTPUT
              echo "is_deployable=false" >> $GITHUB_OUTPUT
              echo "deploy_env=beta" >> $GITHUB_OUTPUT
              echo "检测到 merge ref，使用 PR #${PR_NUMBER} 的 merge ref"
            else
              echo "ref=main" >> $GITHUB_OUTPUT
              echo "pr_number=" >> $GITHUB_OUTPUT
              echo "is_deployable=true" >> $GITHUB_OUTPUT
              echo "deploy_env=$DEPLOY_ENV" >> $GITHUB_OUTPUT
              echo "无法提取 PR 号，使用默认分支: main (原始 ref: $REF)"
            fi
          elif [[ "$REF" =~ ^(web-)?v[0-9]+\.[0-9]+\.[0-9]+ ]] || [[ "$REF" =~ ^refs/tags/ ]]; then
            echo "ref=$REF" >> $GITHUB_OUTPUT
            echo "pr_number=" >> $GITHUB_OUTPUT
            echo "is_deployable=true" >> $GITHUB_OUTPUT
            echo "deploy_env=$DEPLOY_ENV" >> $GITHUB_OUTPUT
            echo "检测到 tag: $REF，将执行部署"
          else
            echo "ref=$REF" >> $GITHUB_OUTPUT
            echo "pr_number=" >> $GITHUB_OUTPUT
            echo "is_deployable=false" >> $GITHUB_OUTPUT
            echo "deploy_env=$DEPLOY_ENV" >> $GITHUB_OUTPUT
            echo "使用指定 ref: $REF"
          fi

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          repository: leaperone/2SOMEone
          ref: ${{ steps.set-ref.outputs.ref }}

      - name: Check Skip CI
        run: |
          if grep -q "noci" <<< "${{ github.event.client_payload.message || '' }}"; then
            echo "跳过构建: commit message 包含 'noci'"
            exit 78
          fi

      - name: Setup docker-compose
        uses: KengoTODA/actions-setup-docker-compose@v1
        with:
          version: "2.14.2"

      - name: Build Docker images
        env:
          POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY }}
          POSTHOG_2SOMEONE_ENV_ID: ${{ secrets.POSTHOG_2SOMEONE_ENV_ID }}
          NEXT_PUBLIC_POSTHOG_HOST: https://us.posthog.com
        run: docker compose -f apps/web/docker/docker-compose-build.yml build

      - name: Login to Aliyun Docker Registry (Shanghai)
        if: steps.set-ref.outputs.is_deployable == 'true'
        uses: docker/login-action@v3
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_DOCKER_USERNAME }}
          password: ${{ secrets.ALIYUN_DOCKER_PASSWORD }}

      - name: Push Docker images
        if: steps.set-ref.outputs.is_deployable == 'true'
        run: docker push registry.cn-shanghai.aliyuncs.com/leaperone/2someren:web-main-latest

  # TODO: Database migration
  # DB is on PVE LXC 210 (internal network 10.10.10.210), not reachable from GitHub Actions.
  # Planned approach: SSH to app LXC and run migration via Docker.
  # Requires adding a migration target to the Dockerfile or installing Node.js on the LXC.

  deploy:
    needs: build-and-push
    if: needs.build-and-push.outputs.is_deployable == 'true'
    uses: ./.github/workflows/deploy-pve.yml
    with:
      project: twosomeone
      env: ${{ needs.build-and-push.outputs.deploy_env }}
    secrets: inherit

  pr-comment:
    needs: [build-and-push, deploy]
    if: always() && needs.build-and-push.outputs.pr_number != ''
    uses: leaperone/leaperone-releases/.github/workflows/pr-comment.yml@main
    with:
      status: ${{ needs.deploy.result == 'skipped' && needs.build-and-push.result || needs.deploy.result }}
      ref: leaperone/2SOMEone@${{ github.event.client_payload.ref }}
      run_number: ${{ github.run_number }}
      message: ${{ github.event.client_payload.message }}
      author: ${{ github.event.client_payload.author }}
      run_id: ${{ github.run_id }}
      pr_number: ${{ needs.build-and-push.outputs.pr_number }}
      repository: leaperone/2SOMEone
    secrets:
      ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}

  notify:
    needs: [build-and-push, deploy]
    if: always()
    uses: leaperone/leaperone-releases/.github/workflows/feishu-notification.yml@main
    with:
      status: ${{ needs.deploy.result == 'skipped' && needs.build-and-push.result || needs.deploy.result }}
      ref: leaperone/2SOMEone@${{ github.event.client_payload.ref }}
      run_number: ${{ github.run_number }}
      message: ${{ github.event.client_payload.message }}
      author: ${{ github.event.client_payload.author }}
      run_id: ${{ github.run_id }}
      repository: leaperone/2SOMEone
      pr_number: ${{ needs.build-and-push.outputs.pr_number }}
    secrets:
      FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
