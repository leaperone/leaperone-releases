name: Deploy LEAPERone
run-name: >-
  🚀 Deploy LEAPERone · ${{ github.event.client_payload.env || inputs.env || 'production' }} · ${{ github.event.client_payload.message || inputs.message || '' }} · @${{ github.event.client_payload.author || github.actor }} [src=${{ github.event.client_payload.source_sha || github.sha }}][env=${{ github.event.client_payload.env || inputs.env || 'production' }}]

on:
  repository_dispatch:
    types: [deploy-leaperone]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    outputs:
      pr_number: ${{ steps.set-ref.outputs.pr_number }}
      is_deployable: ${{ steps.set-ref.outputs.is_deployable }}

    steps:
      - name: Validate and set ref
        id: set-ref
        run: |
          REF="${{ github.event.client_payload.ref }}"
          if [ -z "$REF" ]; then
            echo "ref=main" >> $GITHUB_OUTPUT
            echo "pr_number=" >> $GITHUB_OUTPUT
            echo "is_deployable=true" >> $GITHUB_OUTPUT
            echo "使用默认分支: main (ref 为空)"
          elif [[ "$REF" == "main" ]] || [[ "$REF" == "refs/heads/main" ]]; then
            echo "ref=$REF" >> $GITHUB_OUTPUT
            echo "pr_number=" >> $GITHUB_OUTPUT
            echo "is_deployable=true" >> $GITHUB_OUTPUT
            echo "使用 main 分支: $REF"
          elif [[ "$REF" =~ ^([0-9]+)/merge ]] || [[ "$REF" == *"/merge"* ]]; then
            echo "ERROR: deploy-leaperone is deploy-only and does not accept PR merge refs. Got ref: $REF" >&2
            exit 1
          elif [[ "$REF" =~ ^(web-)?v[0-9]+\.[0-9]+\.[0-9]+ ]] || [[ "$REF" =~ ^refs/tags/ ]]; then
            # Tag 格式，标记为需要部署（支持 v*, web-v* 前缀）
            echo "ref=$REF" >> $GITHUB_OUTPUT
            echo "pr_number=" >> $GITHUB_OUTPUT
            echo "is_deployable=true" >> $GITHUB_OUTPUT
            echo "检测到 tag: $REF，将执行部署"
          else
            echo "ERROR: deploy-leaperone only accepts deployable refs (main or release tags). Got ref: $REF" >&2
            exit 1
          fi

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          repository: leaperone/LEAPERone
          ref: ${{ steps.set-ref.outputs.ref }}

      - name: Check Skip CI
        run: |
          if grep -q "noci" <<< "${{ github.event.client_payload.message || '' }}"; then
            echo "跳过构建: commit message 包含 'noci'"
            exit 78
          fi

      - name: Set Sentry release
        id: sentry
        run: echo "release=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT

      - name: Validate Sentry source map env
        if: steps.set-ref.outputs.is_deployable == 'true'
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
        run: |
          test -n "$SENTRY_AUTH_TOKEN" || {
            echo "SENTRY_AUTH_TOKEN is required for deployable web builds"
            exit 1
          }

      - name: Setup docker-compose
        uses: KengoTODA/actions-setup-docker-compose@v1
        with:
          version: '2.14.2'

      - name: Build Docker images
        run: docker compose -f ./docker/docker-compose-build.yml build
        env:
          NEXT_PUBLIC_POSTHOG_KEY: ${{ vars.LEAPERONE_POSTHOG_KEY }}
          NEXT_PUBLIC_POSTHOG_HOST: ${{ vars.LEAPERONE_POSTHOG_HOST }}
          NEXT_PUBLIC_SENTRY_DSN: https://83a80750104b2b1be836f4eceac80d02@sentry.leaperone.cn/4
          SENTRY_ORG: leaperone
          SENTRY_PROJECT: leaperone-web
          SENTRY_URL: https://sentry.leaperone.cn
          SENTRY_RELEASE: ${{ steps.sentry.outputs.release }}
          SENTRY_AUTH_TOKEN: ${{ steps.set-ref.outputs.is_deployable == 'true' && secrets.SENTRY_AUTH_TOKEN || '' }}

      - name: Login to Aliyun Docker Registry
        if: steps.set-ref.outputs.is_deployable == 'true'
        uses: docker/login-action@v3
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_DOCKER_USERNAME }}
          password: ${{ secrets.ALIYUN_DOCKER_PASSWORD }}

      - name: Push Docker images
        if: steps.set-ref.outputs.is_deployable == 'true'
        run: |
          docker push registry.cn-hongkong.aliyuncs.com/leaperone/leaperone:web-latest
          docker push registry.cn-hongkong.aliyuncs.com/leaperone/leaperone:api-latest

      - name: Setup Node.js
        if: steps.set-ref.outputs.is_deployable == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install pnpm
        if: steps.set-ref.outputs.is_deployable == 'true'
        uses: pnpm/action-setup@v4
        with:
          version: 9
      - name: Run Database Migrations
        if: steps.set-ref.outputs.is_deployable == 'true'
        run: |
          pnpm install --filter @leaperone/db
          DATABASE_URL="${{ secrets.LEAPERONE_DATABASE_URL }}" pnpm --filter @leaperone/db db:migrate

      - name: Deploy to Production
        if: steps.set-ref.outputs.is_deployable == 'true'
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.ALIYUN_HK_HOST }}
          username: ${{ secrets.ALIYUN_CN_USERNAME }}
          port: ${{ secrets.ALIYUN_CN_PORT }}
          key: ${{ secrets.ALIYUN_CN_SSH_PRIVATE_KEY }}
          script: ${{ secrets.DEPLOY_LEAPERONE_SCRIPT }}
